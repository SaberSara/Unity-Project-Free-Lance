using UnityEngine;
using System.Collections;

public class PlayerNameSinglePlayer : MonoBehaviour {

    private FacebookConnection fbc;

    // Use this for initialization
    void Start()
    {
//        fbc = (FacebookConnection)FindObjectOfType(typeof(FacebookConnection));
//        fbc.ShowLogedInUserName(this.transform.GetChild(0).GetComponent<UILabel>());
    }
}
